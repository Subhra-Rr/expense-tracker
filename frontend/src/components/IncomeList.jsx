import { formatCurrency, formatDate } from '../utils/expenseOptions'

function IncomeList({ incomes, loading, error, deletingId, onEdit, onDelete }) {
  return <section className="mt-8 rounded-2xl border border-slate-800/90 bg-slate-900/80 p-5 shadow-lg shadow-black/10 sm:p-8"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-emerald-700">Income history</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-white">Your income</h2></div><span className="text-xs text-slate-500">{incomes.length} shown</span></div>{error && <p className="mt-5 rounded-xl border border-red-900/80 bg-red-950/40 p-3 text-sm text-red-200" role="alert">{error}</p>}{loading ? <p className="py-12 text-center text-sm text-slate-500">Loading income...</p> : incomes.length === 0 ? <p className="py-12 text-center text-sm text-slate-500">No income records yet.</p> : <div className="mt-6 divide-y divide-slate-200">{incomes.map((income) => <article className="flex flex-col gap-3 py-4 first:pt-0 sm:flex-row sm:items-center sm:justify-between" key={income._id}><div><p className="font-semibold text-white">{income.source}</p><p className="mt-1 text-sm text-slate-500">{formatDate(income.date)} - {income.category} - {income.account?.name || 'Account unavailable'}</p></div><div className="flex items-center justify-between gap-5 sm:justify-end"><p className="font-semibold text-emerald-700">+{formatCurrency(income.amount)}</p><button className="text-sm font-semibold text-emerald-700" type="button" onClick={() => onEdit(income)}>Edit</button><button className="text-sm font-medium text-red-600" type="button" disabled={deletingId === income._id} onClick={() => onDelete(income)}>{deletingId === income._id ? 'Deleting...' : 'Delete'}</button></div></article>)}</div>}</section>
}

export default IncomeList
